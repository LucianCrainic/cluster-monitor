# This is the only non-secret launcher override. Both services intentionally
# remain fixed to their documented loopback addresses and ports.
CLUSTER_MONITOR_CONFIG=config/clusters.yaml

# Usually leave this unset: Vite proxies relative /api requests to the backend.
# VITE_API_BASE_URL=http://127.0.0.1:8000/api
